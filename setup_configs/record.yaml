robot:
  type: so101_follower
  port: /dev/tty.usbmodem5A680122821
  id: right_follower
  cameras:
    bottom_cam:
      type: opencv
      index_or_path: 0
      width: 800
      height: 600
      fps: 30
    side_cam:
      type: opencv
      index_or_path: 1
      width: 800
      height: 600
      fps: 30
    top_cam:
      type: opencv
      index_or_path: 2
      width: 800
      height: 600
      fps: 30

teleop:
  type: so101_leader
  port: /dev/tty.usbmodem5A680110941
  id: right_leader

display_data: true

dataset:
  num_episodes: 5
  video_encoding_batch_size: 5
  single_task: "Insert wire tip upward through black square from below"
  episode_time_s: 40
  repo_id: eliasab16/insert_wire_prompt_1

resume: true